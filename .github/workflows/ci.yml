name: CI

on:
  pull_request:
    branches: [dev, main]
  push:
    branches: [dev, main]
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: ci-${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  quality:
    name: Quality gate
    runs-on: ubuntu-24.04
    timeout-minutes: 12
    steps:
      - name: Check out repository
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false

      - name: Install pinned toolchain
        uses: paradoxum-games/setup-rokit@29e6ee09651a3711e3a287a1037671d7b74db6d0 # v3
        with:
          version: 1.2.0
          cache: true

      - name: Install pinned Luau CLI
        run: scripts/install-luau-cli.sh

      - name: Install pinned ShellCheck
        run: bash scripts/install-shellcheck.sh

      - name: Bootstrap Rayfield vendor snapshot
        if: github.event_name == 'pull_request' && github.head_ref == 'refactor/vendor-rayfield'
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          git fetch origin "$GITHUB_HEAD_REF"
          git switch -C "$GITHUB_HEAD_REF" "origin/$GITHUB_HEAD_REF"
          bash scripts/vendor-rayfield.sh
          if git diff --quiet -- vendor/rayfield-gen2/bundled.luau; then
              exit 0
          fi
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git add vendor/rayfield-gen2/bundled.luau
          git commit -m "vendor: pin Rayfield Gen2 1.1.0"
          git push "https://x-access-token:${GH_TOKEN}@github.com/${GITHUB_REPOSITORY}.git" "HEAD:${GITHUB_HEAD_REF}"

      - name: Validate, test, and build
        run: scripts/ci.sh

      - name: Validate workflows
        env:
          ACTIONLINT_SHA256: 8aca8db96f1b94770f1b0d72b6dddcb1ebb8123cb3712530b08cc387b349a3d8
          ACTIONLINT_URL: https://github.com/rhysd/actionlint/releases/download/v1.7.12/actionlint_1.7.12_linux_amd64.tar.gz
        run: |
          archive="${RUNNER_TEMP}/actionlint.tar.gz"
          install_dir="${RUNNER_TEMP}/actionlint"
          mkdir -p "$install_dir"
          curl \
              --connect-timeout 15 \
              --fail \
              --location \
              --max-time 120 \
              --retry 3 \
              --retry-all-errors \
              --silent \
              --show-error \
              "$ACTIONLINT_URL" \
              --output "$archive"
          printf '%s  %s\n' "$ACTIONLINT_SHA256" "$archive" | sha256sum --check --strict
          tar --no-same-owner -xzf "$archive" -C "$install_dir" actionlint
          "$install_dir/actionlint"
