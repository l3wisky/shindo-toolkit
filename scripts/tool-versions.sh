#!/usr/bin/env bash

# Keep standalone tool versions in one place so installers and CI agree.
# The constants appear unused here because they are consumed by sourcing scripts.
# shellcheck disable=SC2034
readonly ROKIT_VERSION="1.2.0"
readonly LUAU_VERSION="0.733"
readonly SHELLCHECK_VERSION="0.11.0"
